apiVersion: v1
kind: Service
metadata:
  name: rectangles-api
  labels:
    app: rectangles-api
spec:
  type: LoadBalancer
  selector:
    app: rectangles-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080